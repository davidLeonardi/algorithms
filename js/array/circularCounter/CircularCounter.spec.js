const assert = require('assert');
// const CircularCounter = require('./CircularCounter');

describe('array', function() {
    describe('circularCounter', function() {
        it('should be able to start a test', function() {
            assert.equal(true, true);
        });
    });
});
